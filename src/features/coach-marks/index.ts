// v1.0 GA — coach marks: inline, dismissable, contextual hints.
//
// Replaces the "show every tip on first launch" pattern with
// "one tip, where the user is, dismissable forever in one click".

export { CoachMark } from "./CoachMark";
export type { CoachMarkProps } from "./CoachMark";
export { useCoachMarks } from "./store";
